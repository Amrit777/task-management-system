# Root .env for docker-compose. Copy to .env and fill in. Do NOT commit .env.
# Generate a strong secret:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
DB_PASSWORD=tms_password
